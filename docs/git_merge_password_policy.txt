# --- 🚀 Feature Branch Workflow for Password Policy ---

# 1. 🏗️ Create and switch to your feature branch
git checkout -b feature/password-policy

# 2. 🛠️ Make your code changes (e.g., implement password history checks)
#    📦 Stage all changes
git add .
#    📝 Commit your changes with a descriptive message
git commit -m "Implement password policy: prevent reuse of last 3–5 passwords"

# 3. ⬆️ Push the feature branch to the remote repository
git push origin feature/password-policy

# --- ✏️ If you need to update a specific file in your commit ---

# 4. 🔄 Unstage the file if needed
# git reset HEAD docs/git_merge_password_policy.txt
# Or unstage all changes:
git reset

# 5. 📋 Check the status of your working directory
git status

# 6. 📂 Stage the file again after making changes
git add docs/git_merge_password_policy.txt

# 7. 📝 Commit the updated file with a clear message
git commit -m "Add git workflow guide for password policy feature"

# 8. ⬆️ Push the feature branch again if needed
git push origin feature/password-policy

# --- 🔀 Merge Feature Branch into Working Branch ---

# 9. ↩️ Switch back to your working branch (e.g., dev)
git checkout working-code

# 10. 🔗 Merge the feature branch into your working branch with a clear message
git merge feature/password-policy -m "Merge feature/password-policy into working-code: implement password policy"

# 11. ⬆️ Push the updated working branch to the remote repository
git push origin working-code
