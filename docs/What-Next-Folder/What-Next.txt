1.So, do implement when a user logs in if password for neayby exxiry kind like gst portal site or any any banks internet banking site s like jsut show warning or kind like when that users password will likely going to be expired so if any users password nearby expiry and at login time cherck this if any neayby and show them that date as expiry apssword date so guide or warn user again update like something pleaseee.

so like that.